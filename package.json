{
  "name": "pegu",
  "scripts": {
    "build:dist": "node scripts/build-dist.cjs",
    "deploy:cloudflare": "npm run build:dist && wrangler pages deploy dist --project-name pegu"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
